# 역할별 개발 실행 기록

## 요청과 기준

2026-09-15 사용자 요청에 따라 orchestrator가 GPT-5.6-sol(xhigh)와 Antigravity CLI의 `gemini-3.8-flash-high`를 조율한다. **14:56 사용자 추가 지시: 구현·반복 수정·테스트는 Gemini 우선, GPT는 어려운 설계·오류 분석에 한정한다.** 개발 순서와 완료 판정의 기준은 [개발 계획](plan.md)이다. 시험 구현을 제품 구현 완료로 계산하지 않는다.

착수 시 `D:\org-board`에는 `doc/`의 문서 5개와 커밋이 없는 Git 저장소만 존재했다. 이전 코드의 삭제 원인이나 다른 위치의 존재는 확인되지 않았다. 원본 문서를 첫 Git 커밋으로 보존하고 개발을 시작한다.

## 역할과 수정 경계

| 담당 | 책임 | 초기 소유 파일 |
|---|---|---|
| Orchestrator | 선행 조건 확인, 인터페이스 합의, 통합 검토, 검증, Story 상태와 커밋 관리 | 개발·검증 기록, 통합 수정 |
| GPT-5.6-sol / xhigh | 초기 S00.01·S00.02 결과 인계 후 복잡한 설계·난제 검토만 | 새 구현 작업 배정 중단 |
| Antigravity / gemini-3.8-flash-high / high | 기능 구현·반복 수정·테스트의 기본 담당 | 후속 Story별로 모듈·테스트 소유 경계 배정 |

공유 파일은 동시에 수정하지 않는다. 후속 작업을 배정할 때 모듈별 소유권을 다시 명시한다. 의존성 변경과 최종 통합은 단일 담당자가 처리한다. 실제 직원정보와 사진은 에이전트 입력이나 시험 데이터로 사용하지 않는다.

공유 `dist/`와 Playwright preview 포트 4173을 사용하는 빌드·E2E는 동시에 실행하지 않는다. 담당자는 모듈 구현과 단위시험을 병렬 수행하고 orchestrator가 통합 빌드·브라우저 검사를 순차 실행한다. 개발 화면 확인은 공통 localhost 5173의 `?spike=spreadsheet`, `?spike=imaging` 진입점을 사용한다.

## 병렬 가능한 범위

- **S00.01 내부:** 앱·검증 환경과 자산 출처 조사·다운로드를 병렬 수행한다. MediaPipe WASM 복사는 패키지 설치를 기다린다. 로컬 자산 경로와 manifest 계약을 먼저 공유한다.
- **S00.01 완료 후:** S00.02(XLSX)와 S00.03(이미지·얼굴·좌표)은 서로 독립적이므로 병렬 수행한다. 독립 디렉터리에 구현하고 공용 harness 연결은 순차 통합한다.
- **제품 단계:** 각 Story 내부에서 계약이 확정된 계산 모듈, UI, 검증 fixture를 나눌 수 있다. 선행 Story가 미완료인 제품 기능을 완료했다고 표시하지 않는다.
- **리뷰:** 한 담당자의 구현이 끝나면 다른 담당자가 읽기 전용으로 리뷰할 수 있다. 같은 파일의 동시 수정은 금지한다.

## 순차 실행과 중단 조건

```text
S00.01
  ├─ S00.02 XLSX 시험 ──────────────┐
  └─ S00.03 사진·좌표 → S00.04 PDF·실물 인쇄
                                  ↓
                        S00.05 오프라인·성능
                                  ↓
                        S00.06 디자인·기준 확인
                                  ↓
E01 → E02 → E03 → E04 → E05 → E06 → E07 → E08
```

1. S00.04는 S00.03 완료 후 시작한다. 실제 프린터의 100% 인쇄와 ±0.5mm 실측 없이 완료하지 않는다.
2. S00.05는 S00.02와 S00.04 완료가 필요하다. 자동 시험 성공으로 실물 인쇄 조건을 대체하지 않는다.
3. S00.06에서 잠정 카드 내부 치수, 글꼴 지원, 화면 판단과 FRD 확인 상태를 증거에 따라 기록한다.
4. M0 통과 이전에는 시험용 harness만 개발한다. 이후 각 Epic의 Story 순서는 plan.md §4를 따른다.
5. 사용자 판단이나 실제 장비가 필요한 경우, 먼저 확인 가능한 PDF·시안·측정 안내를 준비하고 필요한 입력과 재개 조건을 기록한다.

## 실행 증적

- `agy.exe models`에서 `gemini-3.8-flash-high` 제공 확인.
- GPT 담당은 full-history fork 대신 모델과 추론 강도를 명시한 독립 sub-agent로 생성했다.
- Antigravity는 `--model gemini-3.8-flash-high --effort high`로 실행한다. 최초 headless 실행의 읽기 권한 거부 후, 작업 폴더·수정 범위를 제한한 지시와 CLI 자동 도구 승인 옵션으로 재실행했다. 전역 설정은 변경하지 않는다.
- 검사 결과와 남은 조건은 [verification.md](verification.md)에 기록한다. 체크박스는 모든 완료 기준을 충족했을 때만 변경한다.

## 2026-09-15 14:47 초기 잔여 시간 추정

- 완료 1개, 진행 중 2개, 미착수 27개. 남은 규모는 M 20개 / L 9개다.
- M 20~40분, L 45~90분을 가정하면 순수 개발·검증 시간은 약 13~27시간이다. 확정 납기나 실제 측정치가 아니며, S00.02·03 실측 후 갱신한다.
- 현재 두 시험 마무리 30~90분, E00 기술 검증·시안 준비까지 2~5시간으로 추정했다. 이들은 전체 추정에 포함되는 구간이며 서로 더하지 않는다.
- 실제 인쇄·시안 확인 대기 시간은 제외했다. 선행 조건이 많아 두 모델 사용을 전체 시간 50% 단축으로 계산하지 않는다.

## 2026-09-15 Gemini 중심 후속 배정

- S00.03 완료 커밋: c7574de. 전체 Story 3/30 완료, 27개 미완료.
- S00.04 구현: Antigravity CLI gemini-3.8-flash-high / high. PDF·이름 배치·시험 harness·관련 테스트 담당.
- Orchestrator: 선행 조건 판정과 독립 PDF 엔진을 이용한 치수·렌더링 검사 담당. 구현과 검사 준비는 독립적으로 병렬 진행한다.
- GPT 추가 구현 에이전트는 현재 가동하지 않는다. 고난도 검토가 필요할 때만 배정한다.
- 출력물을 먼저 만들고 실제 인쇄 측정값을 요청한다. 물리 측정 전 S00.04를 완료하지 않는다.

## 2026-09-15 15:25 사용자 긴급 지시 반영 및 신속 구현 정책

- **사용자 지시 원문:** `'테스트는 무시하고 전부 gemini가 빠르게 구현하고 알려줘'`
- **검증 게이트 조정:** 단위 시험(Vitest), E2E(Playwright), 물리 인쇄 실측(±0.5mm) 및 디자인 시안 게이트 검증을 잠정 보류(deferred)하고 제품 전체의 신속한 구현을 우선 진행한다.
- **구현 및 검증 현황 정밀 분리:**
  - **정식 검증 완료 (Verified):** 선행 3개 Story(S00.01, S00.02, S00.03)만 정식 검증 완료 상태를 유지한다.
  - **구현 완료 및 최종 통합 완료 / 검증 보류 (Implemented, Not Verified):** Product 담당 Gemini가 E01~E06 전체 제품 기능 구현을 완료하고 최종 통합 완료이며, 로컬 실행/종료 패키징(S07.03) 또한 완료되었다. 단, 사용자 지시에 따라 검증 게이트가 보류되었으므로 이들은 '미검증(not verified)' 상태로 정확히 기록한다.
- **소유 파일 및 스크립트 구축:**
  - Windows 원클릭 시작/종료 배치 및 PowerShell 스크립트(`scripts/start-local.ps1`, `scripts/stop-local.ps1`, `start-org-board.cmd`, `stop-org-board.cmd`)와 관련 문서(`README.md`, `doc/user-guide.md`, `doc/development-execution.md`, `doc/plan.md`, `doc/verification.md`)를 단독 소유하여 구현 완료.
  - 타 에이전트의 `src/product`, `src/technical/pdf` 수정과 충돌하지 않도록 소유 경계를 엄격히 유지함.
- **서버 커맨드라인 절대경로 및 PID 재사용 방지 설계:**
  - npm 래퍼가 자식 프로세스 커맨드라인에서 작업 경로를 누락하는 문제를 방지하기 위해, `scripts/start-local.ps1`에서 `node.exe`로 `node_modules\vite\bin\vite.js` 절대 경로를 직접 호출하여 WMI `CommandLine`에 항상 절대 작업 디렉터리가 포함되도록 보장함.
  - `tmp/org-board.pid`에 프로세스 ID뿐 아니라 시작 시각(`startTimeUtc`)을 함께 기록하여, Windows가 PID를 다른 무관한 프로세스에 재할당하더라도 오종료하지 않도록 방어함.
- **번들 글꼴 확정:**
  - 실제 번들 인쇄 글꼴은 **나눔고딕 ExtraBold 800 (`public/assets/fonts/NanumGothic-ExtraBold.ttf`)**이며, 일본어 고유 문자(히라가나/가타카나/일본식 한자)는 미지원으로 문서화함.
- **안전한 실행 및 데이터 보호:**
  - 포트 4173 충돌 시 작업 영역 소유 프로세스만 종료하며 무관한 외부 프로세스는 절대 종료하지 않음. 영구 사용자 데이터는 보관하지 않음.


