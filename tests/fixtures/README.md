# Synthetic fixtures

Only generated, clearly labelled test data belongs in this directory. Real employee names, photos, workbooks, file paths, or other personal information must never be added.

The S00.01 default fixture lives in `src/technical/fixture.ts` so the browser harness and automated tests validate the same input.
